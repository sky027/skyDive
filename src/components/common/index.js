import Vue from 'vue'

// 弹窗
import ComDailog from "./cdialog/js";
Vue.use(ComDailog);

// 按钮
import ComBtn from './button';
Vue.use(ComBtn);

// 分割线
import SplitLine from './splitLine';
Vue.use(SplitLine);
